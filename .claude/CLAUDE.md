# django-mosaic

This is a reusable Django app (installed as a package into other projects), not a standalone project. Any templates, CSS, etc. are just there for illustration / testing purposes. The consumers of mosaic will likely overwrite them.
