[Unit]
Description=Hourly backup of {{APP_NAME}} database
Requires={{APP_NAME}}-backup.service

[Timer]
# Run every hour on the hour
OnCalendar=hourly
Persistent=true

[Install]
WantedBy=timers.target
